# Directories to search for child directories (shown in fzf).
# Suffix with :N to control search depth (default: 1).
TS_SEARCH_PATHS=(~/projects)

# Directories added as-is (no traversal).
TS_EXPLICIT_PATHS=(~/.dotfiles)
